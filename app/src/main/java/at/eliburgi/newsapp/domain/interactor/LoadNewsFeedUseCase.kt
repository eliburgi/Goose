package at.eliburgi.newsapp.domain.interactor

/**
 * Created by Elias on 31.12.2017.
 */
class LoadNewsFeedUseCase {
    /*val FEED_REQUEST_ID = 0

    fun loadFeed() {
        val topRequest = TopArticleRequest(FEED_REQUEST_ID)
        val recentRequest = RecentArticleRequest(FEED_REQUEST_ID)

        articleRepository.getTopArticles(topRequest)
        // merge with
        articleRepository.getRecentArticles(recentRequest)
        // filter to make sure no two identical articles (same id) are returned
        // return single
    }

    fun loadMore() {
        articleRepository.getMoreTopArticles(FEED_REQUEST_ID)
        // merge with
        articleRepository.getMoreRecentArticles(FEED_REQUEST_ID)
        // filter to make sure no two identical articles (same id) are returned
        // return single
    }*/
}